클래스 만들려면 필드랑 메소드 필요 
필드(멤버변수), 메소드(멤버함수)는 클래스의 멤버
   
      나무 클래스
속성   종, 키     
       나이, 색상   필드
동작   흔들린다     메소드


필드는 초기화하지 않아도 무방
지역변수는 무조건 초기화 해야함



객체지향프로그래밍
 객체를 추가적으로 프로그래밍해준다


중간 점검 문제
1.클래스
2.
myDog에 포만감이 있다면 yourDog에도 포만감이 존재? 아님 없음?
메소드공유 필드는 별개로 따짐 
3.필드와 메소드
4.도트연산자
5.필드
6.

1.실제값이 저장되느냐 함수값이 저장되느냐의 차이
2.
3.null를 넣어준다.



필드의 초기화
초기화하지 않은 지역변수는 컴파일에러남
필드로 선언되었다면 0출력

setter getter 왜 사용?
setter만을 제공하면 자동적으로 읽기만 가능한 필드를 만들 수 있다.
->getter만을 제공하면~(강의자료 오류)

문자열 비교
if(a.equals(b))